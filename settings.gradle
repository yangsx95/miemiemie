rootProject.name = 'miemiemie'

void includePath(String path) {
    include path
    findProject(path)?.name = path.substring(path.lastIndexOf(':') >= 0 ? path.lastIndexOf(':') : 0)
}


includePath 'miemiemie-core'
includePath 'miemiemie-starter-web'
includePath 'miemiemie-starter-feign'
includePath 'miemiemie-starter-mybatisplus'
includePath 'miemiemie-starter-aliyunoss'
includePath 'miemiemie-starter-xxljob'

includePath 'miemiemie-demo'

